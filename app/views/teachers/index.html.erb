<h1>My Students</h1>
<% @students.each do |student| %>
  <p><%= link_to "#{student.first_name} #{student.last_name}", lesson_index_path(student) %></p>
  <p><%= student.age %> <%= student.instrument %> </p>
<% end %>
